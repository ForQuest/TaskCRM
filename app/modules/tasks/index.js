import * as Actions from './actions';
import * as Components from './components';
import * as Reducers from './reducers';
import * as Types from './types';

const Tasks = { Actions, Components, Reducers, Types };

export default Tasks;
